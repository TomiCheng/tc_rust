[package]
name = "tc_tbc_pad"
description = "Trailing bit complement block padding for the tc_rust workspace."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tc_crypto = { version = "0.1", path = "../../tc_crypto" }
tc_pad = { version = "0.1", path = "../../tc_pad" }
