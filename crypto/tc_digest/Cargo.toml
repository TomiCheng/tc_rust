[package]
name = "tc_digest"
description = "Shared message-digest contracts for the tc_rust workspace."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
