[package]
name = "tc_keccak"
description = "Keccak, SHA-3, SHAKE, cSHAKE, ParallelHash, and TupleHash for the tc_rust workspace."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tc_digest = { version = "0.1", path = "../../tc_digest" }
