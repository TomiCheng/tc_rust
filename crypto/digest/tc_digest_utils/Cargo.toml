[package]
name = "tc_digest_utils"
description = "Message-digest adapters for the tc_rust workspace."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tc_digest = { version = "0.1", path = "../../tc_digest" }

[dev-dependencies]
tc_sha = { version = "0.1", path = "../tc_sha" }
