[package]
name = "tc_haraka"
description = "Haraka digest implementations for the tc_rust workspace."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
tc_digest = { version = "0.1", path = "../../tc_digest" }
