[package]
name = "tc_block_modes"
description = "Block cipher modes of operation ported from the Bouncy Castle C# library as a learning project."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tc_cipher_core = { path = "../tc_cipher_core" }

[dev-dependencies]
# 只有測試需要具體 engine；mode 本身僅依賴 trait crate。
tc_block_cipher = { path = "../tc_block_cipher" }
