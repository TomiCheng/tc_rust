[package]
name = "tc_math"
description = "A pure-Rust big-integer and number-theory library, ported from the Bouncy Castle C# library as a learning project."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
rand_core = { workspace = true }

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "mod_pow"
harness = false
