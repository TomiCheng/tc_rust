[package]
name = "tc_macs"
description = "Message authentication code (MAC) implementations ported from the Bouncy Castle C# library as a learning project."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tc_cipher_core = { path = "../tc_cipher_core" }
tc_crypto_core = { path = "../tc_crypto_core" }

[dev-dependencies]
tc_block_cipher = { path = "../tc_block_cipher" }
