[package]
name = "tc_stream_cipher"
description = "Stream cipher implementations ported from the Bouncy Castle C# library as a learning project."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
tc_crypto_core = { path = "../tc_crypto_core" }
