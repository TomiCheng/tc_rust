[package]
name = "tc_digest"
description = "Message-digest algorithms (MD2, …), ported from the Bouncy Castle C# library as a learning project."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
tc_crypto_core = { path = "../tc_crypto_core" }

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "blake2b"
harness = false
