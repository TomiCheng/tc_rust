[package]
name = "tc_aead_cipher"
description = "Authenticated encryption with associated data (AEAD) ciphers ported from the Bouncy Castle C# library as a learning project."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tc_cipher_core = { path = "../tc_cipher_core" }
