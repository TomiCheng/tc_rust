[package]
name = "tc_block_cipher"
description = "Block cipher implementations ported from the Bouncy Castle C# library as a learning project."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
force-portable-aes = []

[dependencies]
tc_cipher_core = { path = "../tc_cipher_core" }

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "aes"
harness = false
