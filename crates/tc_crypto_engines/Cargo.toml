[package]
name = "tc_crypto_engines"
description = "Cryptographic engine implementations ported from the Bouncy Castle C# library as a learning project."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
