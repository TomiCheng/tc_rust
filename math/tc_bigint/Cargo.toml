[package]
name = "tc_bigint"
description = "A pure-Rust arbitrary-precision integer library ported from Bouncy Castle C#."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
rand_core = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "mod_pow"
harness = false
