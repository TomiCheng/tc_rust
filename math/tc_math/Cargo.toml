[package]
name = "tc_math"
description = "Pure-Rust finite-field and elliptic-curve foundations ported from Bouncy Castle C#."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = ["tc_bigint/std"]

[dependencies]
rand_core = { workspace = true }
tc_bigint = { version = "0.1.0", path = "../tc_bigint", default-features = false }
