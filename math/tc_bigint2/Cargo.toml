[package]
name = "tc_bigint2"
description = "Fixed-precision big integer primitives for cryptographic use."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []

[dependencies]
